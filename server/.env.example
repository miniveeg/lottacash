PORT=3001
WEBHOOK_SECRET=dev-secret-change-me

# Optional but recommended for production monitoring + faster RPC
HELIUS_API_KEY=

# Override RPC (defaults to Helius RPC if key set, else public mainnet)
# SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# Background wallet poller
MONITOR_ENABLED=true
MONITOR_INTERVAL_MS=45000
